{
  "name": "stickit-avatar-creator",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "build-wrangler-local": "turbo run build-wrangler-local",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types"
  },
  "devDependencies": {
    "typescript": "latest",
    "prettier": "^3.6.2",
    "turbo": "^2.5.6"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
